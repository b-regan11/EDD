# TempQueryName

import os
import subprocess

# Compile the Java class
subprocess.run(['javac', 'FileSelection/FileSelection.java'])

# Run the Java class
subprocess.run(['java', 'FileSelection/FileSelection.java'])


